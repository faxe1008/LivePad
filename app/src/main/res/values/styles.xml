<resources>


    <style name="LivePadThemeDark" parent="Theme.AppCompat">
        <item name="android:fontFamily">@font/roboto</item>
        <item name="android:colorPrimary">@color/primaryDark</item>
        <item name="android:colorAccent">@color/accentDark</item>
        <item name="android:colorBackground">@color/backgroundDark</item>
        <item name="android:textColor">@color/fontColorDark</item>

        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name = "android:windowActionBar">false</item>
        <item name = "android:windowNoTitle">true</item>
    </style>

    <style name="LivePadThemeLight" parent="Theme.AppCompat">
        <item name="android:fontFamily">@font/roboto</item>
        <item name="android:colorPrimary">@color/primaryLight</item>
        <item name="android:colorAccent">@color/accentLight</item>
        <item name="android:colorBackground">@color/backgroundLight</item>
        <item name="android:textColor">@color/fontColorLight</item>

        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name = "android:windowActionBar">false</item>
        <item name = "android:windowNoTitle">true</item>
    </style>
</resources>
